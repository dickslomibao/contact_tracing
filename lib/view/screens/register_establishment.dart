import 'package:flutter/material.dart';

class RegisterEstablishment extends StatelessWidget {
  const RegisterEstablishment({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sign up Establishment'),
      ),
    );
  }
}
